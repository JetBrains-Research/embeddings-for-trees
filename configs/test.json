{
  "paths": {
    "train": "./data/java-test/training_preprocessed",
    "validate": "./data/java-test/validation_preprocessed",
    "vocabulary": "./data/java-test/vocabulary.pkl"
  },

  "hidden_states": {
    "embedding": 64,
    "encoder": 64,
    "decoder": 64
  },

  "embedding": {
    "embeddings": {
      "token": {},
      "type": {}
    },
    "reduction": {
      "name": "sum",
      "params": {}
    }
  },

  "encoder": {
    "name": "TreeLSTM",
    "params": {
      "cell": {
        "name": "MultiHeadAttentionTreeLSTMCell",
        "params": {
          "n_heads": 4,
          "dropout": 0.25
        }
      }
    }
  },

  "decoder": {
    "name": "LSTMDecoder",
    "params": {
      "dropout": 0.5,
      "teacher_force": 0.0,
      "attention": {
        "name": "LuongConcatAttention",
        "params": {}
      }
    }
  },

  "batch_size": 64,
  "n_epochs": 1,
  "lr": 0.1,
  "weight_decay": 1e-4,
  "clip_norm": 5,

  "scheduler": {
    "name": "step",
    "step_size": 500,
    "step_gamma": 0.1
  },

  "evaluation_step": 50,
  "logging_step": 10,
  "logging_folder": "logs",
  "checkpoint_step": -1,
  "checkpoints_folder": "checkpoints"
}
